ext {
    buildId = "${System.getProperty("BUILD_ID")}"
}
task checkEnv {
    doLast {
        println "Here is the build id from Jenkins: ${buildId}"
        for (v in System.getProperties()) {
            println "value: ${v}"
        }
    }
}